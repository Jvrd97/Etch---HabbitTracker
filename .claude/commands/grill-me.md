---
description: "Start an alignment ritual. Forces relentless interview before any planning or coding."
---

Apply the `grill-me` skill to the following input:

$ARGUMENTS

Remember:
- Spawn `explorer` sub-agent ONCE if codebase context is needed
- One question at a time, each with your recommendation
- Walk every branch of the design tree
- Stop only when user says "enough" or all branches are closed
- Do NOT produce a plan — that comes after with `/write-prd`
